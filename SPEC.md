# Vibe 自画像 · 产品规则文档（SPEC）

> 这份文档是 vibe-portrait 这个产品的"宪法"。
> 所有渲染逻辑、章节判断、文案规则、视觉决策都来自这里。
> 改产品规则前，先改 SPEC。

最后更新：2026-05-25

---

## 0. 产品定位（核心约束）

**一句话**：把用户 vibe coding 时的 prompt 历史，做成一张「年度自画像」海报。

### 关键定位决策

| 维度 | 决策 |
|---|---|
| **主语** | **用户**（不是项目）。海报标题永远是「{{user_name}} 的 Vibe 自画像」 |
| **数据范围** | 用户选 1 ~ N 个对话窗口的 user prompt（默认形态就是跨项目） |
| **平台中立** | 不绑定任何 agent 平台，靠 adapter 层抽象 WorkBuddy / Claude Code / Codex 等 |
| **昵称必填** | 安装后第一次跑必须输入昵称（user_name），写入本地配置缓存 |
| **副标识别** | **不要**（不写"@即刻ID""@X"等平台后缀），就用昵称 |
| **故事叙事** | 葬AI 文笔风（反高潮、口语自嘲、对账感），不写"AI 味"装饰文 |
| **产物** | HTML（动态/分享）+ JPG 长图（截图分享） |
| **作家匹配策略** | 永远取最近的（不做反差/拼贴） |
| **章节降级** | 数据不足时**整章跳过**（不凑数），剩余章节连续重新编号 |
| **跨项目隐藏番** | v1 不做（"每个项目里的不同你"） |

---

## 1. 数据流总览

```
[用户对话历史 .jsonl]
        │
        ▼
[adapter 层] ──→ 统一的 Prompt[] 结构
        │
        ▼
[analyze 层] ──→ stats / axes / patterns / quotes / badges
        │
        ▼
[render 层]  ──→ HTML（按章节触发条件渲染 + 重排编号）
        │
        ▼
[screenshot] ──→ JPG 长图
```

---

## 2. 数据结构

### 2.1 Prompt（统一中间结构）

```python
class Prompt(TypedDict):
    text: str              # 用户原始输入（已剥离 system-reminder/user_query 包装）
    timestamp: int         # Unix ms
    project_id: str        # 对话窗口 ID（用于跨项目时区分来源）
    project_label: str     # 用户可读的项目名（取目录或用户输入的别名）
    source: str            # adapter 标识：workbuddy / claude_code / codex
```

### 2.2 Analysis（最终统计产物）

```python
class Analysis(TypedDict):
    stats: dict            # 总览数据（prompts/chars/days/peak）
    axes: tuple            # (length, calm, architect)
    matched_author: dict   # {slug, name_zh, book, ...}
    hour_dist: dict        # {0..23: count}
    weekday_dist: dict     # {Mon..Sun: count}
    daily_dist: dict       # {date: count}
    top_patterns: list     # [(label, count, examples), ...]
    quotes: list           # 番 4 的 5-7 张金句卡
    badges: list           # 番 5 的 4-6 个徽章
    chapters_to_render: list  # 哪些番要渲染，按重排后的连续编号
```

---

## 3. 三轴算法（精确定义）

> 用户的 vibe 性格被映射到 (length, calm, architect) 三维立方体上的一个点，每个轴 ∈ [0,1]。

### 3.1 length 轴（句长偏好）

**含义**：0 = 极短指令派（"好""部署"）；1 = 长铺陈写作派。

**信号源**：`median_len` = 所有 prompt 文本字符长度的中位数。

**为什么用中位数**：极端长 prompt 会拉爆均值，中位数稳定反映"日常 vibe 习惯"。

**公式**：
```
length = clamp(0, 1, log(max(median_len, 5)) / log(300))
```

**校准点**：
| median_len | length |
|---|---|
| 5 字 | 0.00 |
| 15 字 | 0.20 |
| 33 字 | 0.61 |
| 100 字 | 0.81 |
| 300+ 字 | 1.00 |

### 3.2 calm 轴（情绪稳定度）

**含义**：0 = 反复撕裂派；1 = 克制理性派。

**信号源**：`negation_ratio` = 含否定/反复词的 prompt 数 / 总 prompt 数。

**否定词词典**（见 §6 lexicon）：
```
还是 / 不对 / 不好 / 重做 / 重来 / 重新 / 怎么 / 为什么 /
到底 / 算了 / 不行 / 差点意思 / 太...了 / 没啥 / 不咋
```

**公式**：
```
calm = clamp(0, 1, 1 - negation_ratio × 1.7)
```

**校准点**：
| negation_ratio | calm |
|---|---|
| 0% | 1.00 |
| 10% | 0.83 |
| 21% | 0.65 |
| 35% | 0.40 |
| 60%+ | 0.00 |

### 3.3 architect 轴（架构 vs 抠像素）

**含义**：0 = 抠像素工头；1 = 架构师/写作型。

**信号源**：`first_prompt_len`（项目第一句字数） + `long_ratio`（≥200 字 prompt 的占比）。

**公式**：
```
architect = clamp(0, 1, (first_prompt_len / 300) × 0.5 + long_ratio × 8)
```

**校准点**：
| 第一句 | long_ratio | architect |
|---|---|---|
| 10 字 | 0.5% | 0.06 |
| 50 字 | 2% | 0.24 |
| 100 字 | 3% | 0.41 |
| 350 字 | 4% | 0.90 |

---

## 4. 作家匹配（9 人阵容 + 匹配规则）

### 4.1 作家库

存储在 `assets/portraits/_meta.json`，每个作家有：
- 中英文名
- 三轴坐标 `(length, calm, architect)`
- 代表作 + 字数
- tagline / vibe_label
- 600×600 JPG 头像（圆裁处理）

**9 人定位**：

| 作家 | length | calm | architect | 代表作 |
|---|---|---|---|---|
| 海明威 | 0.10 | 0.80 | 0.30 | 《老人与海》6 万 |
| 杜拉斯 | 0.20 | 0.45 | 0.30 | 《情人》5 万 |
| 芥川 | 0.30 | 0.55 | 0.40 | 《罗生门》18 万 |
| 卡夫卡 | 0.35 | 0.20 | 0.40 | 《变形记》5 千 |
| 加缪 | 0.45 | 0.85 | 0.70 | 《局外人》3 万 |
| 博尔赫斯 | 0.55 | 0.75 | 0.90 | 《小径分岔的花园》20 万 |
| 太宰治 | 0.65 | 0.10 | 0.30 | 《人间失格》15 万 |
| 马尔克斯 | 0.85 | 0.65 | 0.85 | 《百年孤独》30 万 |
| 陀思妥 | 0.95 | 0.15 | 0.65 | 《地下室手记》10 万 |

### 4.2 匹配算法

**距离**：欧氏距离
```
d = sqrt((u.length - a.length)² + (u.calm - a.calm)² + (u.architect - a.architect)²)
```

**策略**：永远取距离最小的那个（不做反差/拼贴/前 2 名展示）。

**封面渲染**：
- 圆形头像（130×130，2px 黑色描边）
- caption 三行：作家英文名 / "{name}用 X 字写完了《代表作》" / "你呢 😅"
- 三行以头像中心垂直对齐

---

## 5. 章节体系（核心）

### 5.1 章节清单（共 6 章）

| 番 | 标题 | 副标 | 优先级 |
|---|---|---|---|
| 1 | 先瞧瞧你这张嘴 | 先把账算清楚。 | **P0 必出** |
| 2 | 你的 vibe 生物钟 | 别装勤奋，数据看着呢。 | P1 |
| 3 | 翻来覆去就这几句 | 你自己念一遍试试。 | P1 |
| 4 | 你是这样欺负 AI 的 | AI 没脾气，但有时间戳。 | P2 |
| 5 | 给你的 vibe 颁几个奖 | 认就完事了。 | P2 |
| 终 | 字数是有了，文学是没有 | {{matched_author_zh}}：你呢 😅 | **P0 必出** |

### 5.2 触发条件

#### 番 1（数据墙）— **永远必出**

**最低数据**：≥ 1 条 Prompt
**降级方案**：
- 活跃天数 < 3 时，分母用对话窗口创建到最后一次的天数
- 单日峰值 = Prompt 数（如果只活跃 1 天）

#### 番 2（生物钟）— P1

**触发**：活跃天数 ≥ 2 **且** 有时间戳
**降级方案**：
- 活跃天数 < 7：只显示 24h 热力图，不显示周分布
- 活跃天数 = 1：**整章跳过**

#### 番 3（口头禅 TOP 5）— P1

**触发**：识别到 ≥ 3 个不同口癖词（每个词需出现 ≥ 2 次）
**降级方案**：
- 找到 3-4 个：出 TOP N（不强制 5）
- < 3 个：**整章跳过**

#### 番 4（金句章）— P2

**触发**：≥ 5 条满足金句筛选规则的 prompt
**降级方案**：
- 4 条：出 4 张金句卡（去掉特别奖）
- < 4 条：**整章跳过**

#### 番 5（徽章）— P2

**触发**：能从 15 个候选里凑出 ≥ 4 个徽章
**降级方案**：
- 4-5 个：出徽章卡（不强制 6）
- < 4 个：**整章跳过**

#### 终番（字数对照）— **永远必出**

**最低数据**：总字数 > 0
**永远必出**——这是首尾呼应灵魂。

### 5.3 章节重排规则

**当某章被跳过时，剩余章节按顺序连续重编号**。

例如：番 3 被跳过 →
- 原番 1 → 第 1 番
- 原番 2 → 第 2 番
- ~~原番 3 跳过~~
- 原番 4 → 第 3 番
- 原番 5 → 第 4 番
- 终番 → 终番（编号不变）

### 5.4 不同数据规模的实际形态

| 用户类型 | Prompt 数 | 活跃天数 | 章节 | 海报长度 |
|---|---|---|---|---|
| 重度用户 | 500+ | 17+ | 5 番 + 终番 | 完整 |
| 中型项目 | 100 | 8 | 5 番 + 终番（金句章可能略少） | 完整 |
| 小项目 | 30 | 3 | 番 1/2/3 + 终番 | 4 章（无金句、无徽章） |
| 一次性 | 5 | 1 | 番 1 + 终番 | 极简 2 章 |

**最少保证**：番 1 + 终番。任何用户跑出来至少有这两个。

---

## 6. Lexicon（词典系统）

存储在 `assets/lexicon.json`。

### 6.1 口癖词典（番 3 用）

**预定义 ~30 个候选模式**，按频次排前 5：

```json
{
  "patterns": [
    { "label": "还是…",         "regex": ["还是"] },
    { "label": "我觉得…",        "regex": ["我觉得", "我感觉", "我认为"] },
    { "label": "怎么 / 能不能…",  "regex": ["怎么", "能不能", "能否"] },
    { "label": "不好 / 不对",    "regex": ["^不好", "不好[，。]", "^不对", "不对[，。]"] },
    { "label": "太…了",          "regex": ["太\\w+了", "太\\w+，", "太\\w+。"] },
    { "label": "重做 / 重来 / 重新", "regex": ["重做", "重来", "重新"] },
    { "label": "为什么…",        "regex": ["为什么", "凭什么"] },
    { "label": "是不是…",        "regex": ["是不是"] },
    { "label": "你怎么…",         "regex": ["你怎么"] },
    { "label": "可以吗 / 行不行",  "regex": ["可以吗", "行不行", "能用吗"] },
    { "label": "再来一次",        "regex": ["再来一次", "再试一次", "再来一遍"] },
    { "label": "算了",           "regex": ["算了"] },
    { "label": "不行",           "regex": ["不行"] },
    { "label": "AI 味",          "regex": ["AI 味", "AI味"] },
    { "label": "不要 / 不需要",   "regex": ["不要", "不需要"] }
  ],
  "praise_words": ["非常准确", "完美", "可以", "好的", "牛", "👍"]
}
```

### 6.2 否定词典（calm 轴用）

```json
"negation": [
  "还是", "不对", "不好", "重做", "重来", "重新",
  "怎么", "为什么", "到底", "算了", "不行", "差点意思",
  "没啥", "不咋", "不太行", "不太对"
]
```

### 6.3 表情戏剧词（金句加权用）

```json
"drama_emoji": ["😅", "😂", "😭", "🙄", "🤔", "...", "??"]
```

---

## 7. 金句筛选算法（番 4）

按以下规则**各取 1 句**，最多 7 张卡：

| 槽位 | 规则 |
|---|---|
| 1. 最长一句 | `max(len)`，但 ≤ 500 字（防贴代码） |
| 2. 最短一句 | `min(len)` ≥ 1 字（特别奖：单字 KO） |
| 3. 最强否定 | "你太""你怎么""到底""为什么"开头 + 长度 ≤ 100 |
| 4. 命名时刻 | 含"叫 X 吧""我们就叫""命名为" |
| 5. 含表情戏剧 | 含 `drama_emoji` 任一 |
| 6. 凌晨 1 句 | 0-6 点的 prompt（如果有） |
| 7. 最后一句 | 时间戳最大的（终极态度） |

**每张卡显示**：
- 时间戳（Mono 字体，YYYY.MM.DD · HH:MM）
- 原文（高亮关键词）
- tag（按规则匹配：调侃 AI / AI 味洁癖 / 命名时刻 / 训斥型 / 凌晨派 等）
- ctx（"AI 改半天没改对的标准反应"等触发场景说明，可选）

**单条 > 200 字时截断**：显示前 200 字 + "…"，悬停/链接看全文。

---

## 8. 徽章字典（番 5）

存储在 `assets/badges.json`。**15 个候选**，每个有 trigger 函数：

```json
[
  {
    "id": "friday_warrior",
    "name": "周{X}战神",
    "trigger": "weekday_max_ratio > 0.35",
    "evidence": "{X}% 的 vibe 集中在周{X}，单日峰值 {N} 条。",
    "weight": 10
  },
  {
    "id": "afternoon_coach",
    "name": "下午{X}点教练",
    "trigger": "hour_max_ratio > 0.15",
    "evidence": "{X} 点是单点峰值（{N} 条）。",
    "weight": 9
  },
  {
    "id": "night_owl",
    "name": "夜猫子",
    "trigger": "night_ratio > 0.05",
    "evidence": "0-6 点占比 {X}%，是真夜里干活的人。",
    "weight": 8
  },
  {
    "id": "early_bird",
    "name": "早起鸟",
    "trigger": "morning_ratio > 0.20",
    "evidence": "6-9 点占比 {X}%，是真早起的人。",
    "weight": 8
  },
  {
    "id": "visual_ocd",
    "name": "视觉强迫症",
    "trigger": "(还是 + 太...了) > 30",
    "evidence": "\"还是…\"{N1} 次 + \"太…了\"{N2} 次。",
    "weight": 9
  },
  {
    "id": "ai_taste_clean",
    "name": "AI 味洁癖",
    "trigger": "AI味 + 不是而是 > 3",
    "evidence": "明确点名拒绝 AI 味句式。",
    "weight": 8
  },
  {
    "id": "long_haul",
    "name": "{N} 天 vibe 守夜人",
    "trigger": "active_days >= 10",
    "evidence": "{T} 天里有 {N} 天，你都在 vibe。",
    "weight": 7
  },
  {
    "id": "blitz",
    "name": "{N} 天闪电战",
    "trigger": "active_days <= 5 && total_chars > 10000",
    "evidence": "短时间内输出了 {N} 字。",
    "weight": 8
  },
  {
    "id": "talker",
    "name": "话痨之王",
    "trigger": "mean_len > 100",
    "evidence": "平均句长 {N} 字。",
    "weight": 7
  },
  {
    "id": "minimalist",
    "name": "指令简兵",
    "trigger": "mean_len < 20",
    "evidence": "平均句长 {N} 字，简到极致。",
    "weight": 7
  },
  {
    "id": "redo_master",
    "name": "重做大师",
    "trigger": "重做+重来+重新 > 15",
    "evidence": "\"重做/重来/重新\" {N} 次。",
    "weight": 7
  },
  {
    "id": "multi_owner",
    "name": "跨界主理人",
    "trigger": "project_count >= 3",
    "evidence": "活跃在 {N} 个不同项目里。",
    "weight": 8
  },
  {
    "id": "terminator",
    "name": "无情终结者",
    "trigger": "(好+可以+部署吧+下一步) > 30",
    "evidence": "\"好/可以/部署吧\"类短指令 {N} 次。",
    "weight": 7
  },
  {
    "id": "writer",
    "name": "文字工作者",
    "trigger": "total_chars > 50000",
    "evidence": "总字数 {N} 字，够出本书了。",
    "weight": 7
  },
  {
    "id": "namer",
    "name": "起名大师",
    "trigger": "命名次数 >= 5",
    "evidence": "你给项目里的概念起了 {N} 个名字。",
    "weight": 7
  }
]
```

**选取规则**：
1. 跑所有 trigger，得到候选列表
2. 按 weight 倒序
3. 取前 6 个，但**至少 4 个才出本章**
4. 每个徽章的 name/evidence 用 `{X}` 占位符替换为真实数字

---

## 9. 葬AI 文笔风模板

> 灵感来源：公众号「葬 AI」（葬爱咸鱼）。核心姿态：反高潮、口语自嘲、对账感。

### 9.1 封面引言模板

```
这是 {{user_name}} 的 Vibe 自画像。
没什么深刻洞察，
就是把你这阵子说的话拉出来，跟你对个账。

总共 {{total_chars}} 个字，耗时 {{duration_days}} 天。
```

### 9.2 章节副标（葬AI 风）

| 番 | 副标 | 通用性 |
|---|---|---|
| 1 | 先把账算清楚。 | ✅ |
| 2 | 别装勤奋，数据看着呢。 | ✅ |
| 3 | 你自己念一遍试试。 | ✅ |
| 4 | AI 没脾气，但有时间戳。 | ✅ |
| 5 | 认就完事了。 | ✅ |
| 终 | {{matched_author_zh}}：你呢 😅 | ✅（变量） |

### 9.3 文风规则（用户文案的统一约束）

- **句首不写"在...""在它的..."**等书面化前缀
- **不写"二十四小时分布""按真实出现次数排序"**等图表说明文（极客一看就懂）
- **不写"X 个项目 · X 个徽章 · X 个人"**等机械描述
- **不主动夸用户**（数据自己会说话），但**可主动嘲讽用户**（自嘲）
- 数字用阿拉伯数字（"第 1 番"不写"第一番"）
- 章节编号紧凑无字间疏排（去掉了"画 像 · 一"这种）
- **副标只用一句话**，不分行

### 9.4 锐评写作基准（所有静态/LLM 文案统一遵守）

整个项目的口吻基准 = **葬 AI 风 + 即刻吐槽体**：反高潮 / 自嘲 / 像朋友损你。

**禁忌（凡命中之一立即重写）**：
- ❌ 鸡汤陈述（"命名是设计的一半"/"不连续，但很笃定"）
- ❌ 客观描述（"AI 又翻车了。你没解释，直接训"——像新闻播报）
- ❌ 排比 / 文学化 / 总结式收束
- ❌ 抽象格言（"X 是 Y 的一半"、"放不下的不是项目，是手"——只在结尾收尾时偶尔可用）
- ❌ 主动夸（"很厉害"/"很努力"/"很笃定"）

**鼓励**：
- ✅ 具体动词 + 数字（"折腾了两周""攒了六天怨气"）
- ✅ 反差对比（"3 万字 vs 加缪也 3 万字"）
- ✅ 损人不带脏字（"AI 已被你踢出群聊"）
- ✅ 网络梗 / 口语转折（"反正""说白了""就是"）

**对照例子（学这个调）**：

| ❌ 温柔 | ✅ 锐评 |
|---|---|
| 命名时刻。你给项目里的某个东西起了名字。 | 为了「探测」这俩字，你折腾了两周。 |
| AI 又漏了一期没做。你直接「别偷懒」。 | AI 已被你踢出群聊。 |
| {span} 天里有 {n} 天你都在 vibe。 | {span} 天里 {n} 天没放过它，剩下那几天估计在睡。 |
| 不连续，但很笃定。 | 放不下的不是项目，是手。 |
| 这是难得的好习惯——你的 Mac 也要睡觉。 | 不是不卷，是 Mac 也得睡。 |

> 凡静态 evidence_template / 静态副标 / 静态 desc 落地时，先用上面这张表自检一遍。

---

## 10. 视觉规范（继承自 v2）

详细 CSS 在模板文件里。这里只列产品级决策：

| 项 | 决策 |
|---|---|
| 主色 | 暖白底 #f5f3ee · 墨黑 #111 · Sona 红 #c0392b · Sona 青 #1a6b7a |
| 字体 | Noto Serif SC（标题/正文）+ IBM Plex Mono（数据/编号）+ Cormorant Garamond（英文 italic 副标） |
| 字号层级 | H1 64px / H2 34px / 副标 15px italic / 正文 19px / Mono 11-13px |
| 标题字间距 | 紧凑（letter-spacing < 0.1em） |
| 大字数字 | -3px 字间距，让"168 字"这种数字更紧密 |
| 留白 | 至少 30% |
| 图标 | 仅金句卡左上角的红色 `"` 引号 + 终番 emoji |
| 海报最大宽 | 720px |
| 移动端断点 | 768px（响应式） |
| 截图 | Playwright `locator('.poster').screenshot()` 不截 viewport，避免左右白边 |

---

## 11. 跨 agent adapter 规范

### 11.1 接口定义

```python
class BaseAdapter:
    """所有 agent 的对话历史适配器"""

    name: str            # workbuddy / claude_code / codex
    display_name: str    # "WorkBuddy" / "Claude Code" / "Codex CLI"

    def detect(self) -> bool:
        """检查本机是否安装了这个 agent"""

    def list_projects(self) -> list[ProjectInfo]:
        """列出所有可用的对话窗口（按更新时间倒序）"""

    def extract_prompts(self, project_id: str) -> list[Prompt]:
        """从指定项目抽取所有 user prompt"""
```

### 11.2 已支持平台

| Adapter | 路径 | 特殊处理 |
|---|---|---|
| **workbuddy** | `~/.workbuddy/projects/<dir>/<id>.jsonl` | 剥离 `<system-reminder>` + `<user_query>` 包装 |
| **claude_code** | `~/.claude/projects/<dir>/<id>.jsonl` | 待实现 |
| **codex** | `~/.codex/sessions/<date>/*.jsonl` | 待实现 |

### 11.3 探测顺序

启动时按顺序尝试每个 adapter 的 `detect()`，把所有可用 agent 的项目合并成一个候选列表，每条带 `[源]` 前缀展示。

---

## 12. 用户交互流程

### 12.1 首次启动

```
$ vibe-portrait
（或在 agent 里说："跑我的 vibe 自画像"）

▸ 怎么称呼你？（这个名字会出现在海报封面）
> 恩瑞

> 已记住。下次启动不再询问。配置文件在 ~/.vibe-portrait/config.json

▸ 检测到你的 agent 历史：
  [01] [WB] Sona / 486 条 / 04.02 → 05.24
  [02] [WB] QQ礼物周报 / 234 条 / 05.13 → 05.19
  [03] [CC] 贴纸 benchmark / 89 条 / 05.10 → 05.20

  选哪几个？（输入编号，逗号分隔，回车默认全选）
> 1, 3

▸ 生成中...
✓ 输出：./vibe-portrait/2026-05-25/
  - index.html
  - portrait.jpg (1.5MB)
  - portrait-hd.jpg (2.3MB)
```

### 12.2 后续启动

跳过昵称，直接列项目。

### 12.3 配置文件

```json
// ~/.vibe-portrait/config.json
{
  "user_name": "恩瑞",
  "default_output_dir": "./vibe-portrait",
  "preferences": {
    "skip_under_chars": 100,
    "skip_under_prompts": 5
  }
}
```

---

## 13. 隐私规则

### 13.1 自动脱敏（默认开启）

匹配以下正则的内容自动打码：
- 手机号：`1[3-9]\d{9}` → `1XX****XXXX`
- 邮箱：`[\w.]+@[\w.]+` → `XXX@XXX`
- 身份证：`\d{17}[\dX]` → `XXX...XXX`
- QQ 号（5-11 位纯数字 + 上下文出现"qq""QQ"）→ `XXXXX`

### 13.2 长 prompt 截断

> 200 字的 prompt 在金句章自动截断为前 200 字 + `…`。

### 13.3 数据本地化

**完全本地处理**——不上传任何对话内容到任何服务器。所有产物输出到本地目录。

---

## 14. 后续 v2 规划（不做进 v1）

| 项 | 说明 |
|---|---|
| 跨项目隐藏番（每个项目里的不同你） | 列出 N 个项目分别匹配的作家 |
| 多语言支持（英文/日文 vibe） | 当前只识别中文口癖 |
| 主题切换（深色 / 怀旧报纸） | 当前只有一种主题 |
| Web 版（浏览器内运行） | 隐私敏感，需重写为 JS |
| --anonymize 完全脱敏模式 | 所有金句 hash 处理 |
| 自动发布到 OA Pages / Vercel | 一键部署 |

---

## 文档维护

- 改任何规则前先改 SPEC
- 文案/视觉变动 → 同步更新 §9 / §10
- 新增 adapter → 更新 §11
- 算法迭代 → 更新 §3 / §7 / §8
